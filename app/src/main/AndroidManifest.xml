<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpendSprout_OPSC">
        <!-- Main Launcher Activity -->
        <activity
            android:name=".overview.OverviewActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Screen Activities -->
        <activity
            android:name=".categories.CategoriesActivity"
            android:exported="false" />
        <activity
            android:name=".transactions.TransactionsActivity"
            android:exported="false" />
        <activity
            android:name=".accounts.AccountsActivity"
            android:exported="false" />
        <activity
            android:name=".reports.ReportsActivity"
            android:exported="false" />
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".wants.WantsCategoryActivity"
            android:exported="false" />

        <!-- Edit Activities -->
        <activity
            android:name=".edit.EditCategoryActivity"
            android:exported="false" />
        <activity
            android:name=".edit.EditTransactionActivity"
            android:exported="false" />
        <activity
            android:name=".edit.EditAccountActivity"
            android:exported="false" />

        <!-- Legacy Activity -->
        <activity
            android:name=".CategoryOverviewActivity"
            android:exported="false" />
    </application>

</manifest>